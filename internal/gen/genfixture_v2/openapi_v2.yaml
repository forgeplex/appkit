# Code generated by appkit gen. DO NOT EDIT.
# V2 contract.yaml is the source of truth; streaming shapes use x-appkit-* extensions.
openapi: 3.1.0
info:
  title: "greet V2 契约"
  version: "2"
paths:
  "/v2/ping":
    post:
      operationId: "Ping"
      description: "Checks the V2 unary transport."
      x-appkit-call-shape: "unary"
      x-idempotent: true
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/PingRequestV2"
      responses:
        "200":
          description: 成功
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PingResponseV2"
        default:
          description: 错误（错误身份 = code 扩展成员）
          content:
            application/problem+json:
              schema:
                $ref: '#/components/schemas/Problem'
  "/v2/watch":
    post:
      operationId: "Watch"
      description: "Streams ordered application events."
      x-appkit-call-shape: "server_stream"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/WatchRequestV2"
      responses:
        "200":
          description: SSE response messages; stream errors after response commit are in-band.
          content:
            text/event-stream:
              schema:
                type: string
        default:
          description: 错误（错误身份 = code 扩展成员）
          content:
            application/problem+json:
              schema:
                $ref: '#/components/schemas/Problem'
      x-appkit-stream:
        requestMessage: "WatchRequestV2"
        responseMessage: "WatchResponseV2"
        terminalEvent: "feed.completed"
        cursorField: "event_id"
  "/v2/tail":
    post:
      operationId: "Tail"
      description: "Streams events without an initial request payload."
      x-appkit-call-shape: "server_stream"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
      responses:
        "200":
          description: SSE response messages; stream errors after response commit are in-band.
          content:
            text/event-stream:
              schema:
                type: string
        default:
          description: 错误（错误身份 = code 扩展成员）
          content:
            application/problem+json:
              schema:
                $ref: '#/components/schemas/Problem'
      x-appkit-stream:
        responseMessage: "TailResponseV2"
        cursorField: "sequence"
  "/v2/chat":
    post:
      operationId: "Chat"
      description: "Exchanges bidirectional messages."
      x-appkit-call-shape: "bidi_stream"
      responses:
        "200":
          description: Transport-neutral bidirectional stream; wire transport is defined separately.
        default:
          description: 错误（错误身份 = code 扩展成员）
          content:
            application/problem+json:
              schema:
                $ref: '#/components/schemas/Problem'
      x-appkit-stream:
        requestMessage: "ChatRequestV2"
        responseMessage: "ChatResponseV2"
components:
  schemas:
    EventMetaV2:
      type: object
      description: "Additional application event metadata."
      properties:
        "source":
          type: string
    PingRequestV2:
      type: object
      description: "Ping V2 request DTO."
      properties:
        "name":
          type: string
      required:
        - "name"
    PingResponseV2:
      type: object
      description: "Ping V2 response DTO."
      properties:
        "message":
          type: string
    WatchRequestV2:
      type: object
      description: "Watch V2 request DTO."
      properties:
        "topic":
          type: string
      required:
        - "topic"
    WatchResponseV2:
      type: object
      description: "Watch V2 response DTO."
      properties:
        "event_id":
          type: string
        "event":
          type: string
        "meta":
          $ref: "#/components/schemas/EventMetaV2"
    TailResponseV2:
      type: object
      description: "Tail V2 response DTO."
      properties:
        "sequence":
          type: string
      required:
        - "sequence"
    ChatRequestV2:
      type: object
      description: "Chat V2 request DTO."
      properties:
        "text":
          type: string
      required:
        - "text"
    ChatResponseV2:
      type: object
      description: "Chat V2 response DTO."
      properties:
        "text":
          type: string
    Problem:
      type: object
      description: RFC 9457 problem+json (apperr.Problem).
      properties:
        title:
          type: string
        status:
          type: integer
        code:
          type: string
      required:
        - title
        - status
        - code
