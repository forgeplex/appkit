# Code generated by appkit gen. DO NOT EDIT.
# 事实源是 contract.yaml；本文件是派生导出，供文档与 oasdiff 门禁消费。
openapi: 3.1.0
info:
  title: "greet 契约"
  version: "1"
paths:
  "/greet":
    post:
      operationId: "Greet"
      description: "打招呼（req + resp 形态）。"
      x-idempotent: true
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/GreetRequest"
      responses:
        "200":
          description: 成功
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/GreetReply"
        default:
          description: 错误（错误身份 = code 扩展成员，跨网络重建后 apperr.Is 判定不变）
          content:
            application/problem+json:
              schema:
                $ref: '#/components/schemas/Problem'
  "/stats":
    post:
      operationId: "Stats"
      description: "服务统计（无 req、有 resp 形态）。"
      x-idempotent: true
      responses:
        "200":
          description: 成功
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/StatsReply"
        default:
          description: 错误（错误身份 = code 扩展成员，跨网络重建后 apperr.Is 判定不变）
          content:
            application/problem+json:
              schema:
                $ref: '#/components/schemas/Problem'
  "/reset":
    post:
      operationId: "Reset"
      description: "重置（有 req、仅 error 形态）。"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/ResetRequest"
      responses:
        "200":
          description: 成功
        default:
          description: 错误（错误身份 = code 扩展成员，跨网络重建后 apperr.Is 判定不变）
          content:
            application/problem+json:
              schema:
                $ref: '#/components/schemas/Problem'
  "/ping":
    post:
      operationId: "Ping"
      description: "探活（仅 ctx、仅 error 形态）。"
      x-idempotent: true
      responses:
        "200":
          description: 成功
        default:
          description: 错误（错误身份 = code 扩展成员，跨网络重建后 apperr.Is 判定不变）
          content:
            application/problem+json:
              schema:
                $ref: '#/components/schemas/Problem'
  "/search":
    post:
      operationId: "Search"
      description: "检索分录（命名 DTO 数组响应形态）。"
      x-idempotent: true
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/SearchRequest"
      responses:
        "200":
          description: 成功
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/SearchReply"
        default:
          description: 错误（错误身份 = code 扩展成员，跨网络重建后 apperr.Is 判定不变）
          content:
            application/problem+json:
              schema:
                $ref: '#/components/schemas/Problem'
components:
  schemas:
    Entry:
      type: object
      description: "是分录概要（List 类响应的元素类型；命名 DTO 嵌套封顶一层）。"
      properties:
        "entry_id":
          type: string
        "amount":
          type: string
          description: 十进制数字符串（用 money.Parse 解析，全系统禁 float 金额）
        "at":
          type: string
          format: date-time
      required:
        - "entry_id"
    GreetRequest:
      type: object
      description: "Greet 的请求 DTO。"
      properties:
        "name":
          type: string
      required:
        - "name"
    GreetReply:
      type: object
      description: "Greet 的响应 DTO。"
      properties:
        "message":
          type: string
    StatsReply:
      type: object
      description: "Stats 的响应 DTO。"
      properties:
        "served":
          type: integer
          format: int64
    ResetRequest:
      type: object
      description: "Reset 的请求 DTO。"
      properties:
        "scope":
          type: string
    SearchRequest:
      type: object
      description: "Search 的请求 DTO。"
      properties:
        "prefix":
          type: string
    SearchReply:
      type: object
      description: "Search 的响应 DTO。"
      properties:
        "entries":
          type: array
          items:
            $ref: "#/components/schemas/Entry"
        "next_cursor":
          type: string
    Problem:
      type: object
      description: RFC 9457 problem+json（apperr.Problem）。
      properties:
        type:
          type: string
        title:
          type: string
        status:
          type: integer
        detail:
          type: string
        code:
          type: string
          description: 错误身份；客户端据此重建 *apperr.Error。
        details:
          type: object
      required:
        - title
        - status
        - code
