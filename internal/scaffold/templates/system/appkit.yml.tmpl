# appkit 框架配置：appkit sync / check / dev 读取。
version: 1
# domain 留空 = 组合仓库：只做 wiring + 配置 + 部署，零业务逻辑。
kind: system
domain: ""
module: {{.Module}}
# 合约 module（如 github.com/forgeplex/psp-contracts/go），可空。
contracts: ""
# 额外放行的 forgeplex module。
allowRequires: []
