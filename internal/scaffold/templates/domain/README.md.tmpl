# {{.Name}}

{{.Name}} 业务域仓库，由 `appkit new domain {{.Name}}{{if .Partitioned}} -partitioned{{end}}{{if .Tenant}} -tenant{{end}}` 生成（框架见
github.com/forgeplex/appkit，设计文档 docs/DESIGN.md）。
{{- if .Partitioned}}

**本仓库是分区域域**：一套代码、N 份数据分区，schema 由调用方确定——组合根
注入分区映射（分区键 → schema，见 cmd/{{.Name}}d/main.go 的演示；分区键由
authn.MultiIssuer 按令牌签发方确定，并由已验证 principal 重建进 callctx；
unsigned X-Partition 只负责传输、不授信），
每次事务开启后 `SET LOCAL search_path` 路由到当前分区，查询与迁移因此全部
**无 schema 前缀**。新分区 = 组合根的映射加一条 + 重启，迁移自动建 schema。
`make schema` 自动生成 **logical-template（逻辑模板）**：无前缀迁移只在一次性
临时库的代表 schema `{{.Name}}` 回放一遍，未枚举或检查运行时分区。
{{- end}}
{{- if .Tenant}}

**本仓库是租户域**：{{if .Partitioned}}每个分区内再按行隔离{{else}}单 schema、行级隔离{{end}}——业务表带 `tenant_id` 列并挂
RLS 策略（样例见 `db/migrations/0002_demo_notes.sql`，建真表照抄），
每次事务把租户身份（令牌 tid 焊入 callctx）落成事务级 GUC，策略据此过滤。
隔离在存储层强制：SQL 漏写租户 WHERE 只会查不到行/写入被拒，不再静默泄漏；
漏挂策略的表服务拒绝启动（`pgtx.VerifyTenantRLS`）。跨租户的管理面读路径
（运营看{{if .Partitioned}}本分区{{end}}全部租户）在用例里 `tx.WithReadAllTenants` 放开 SELECT，
写入永远只落当前租户。数据库角色要求**非 superuser 且不带 BYPASSRLS**
（部署清单记得配套）。
{{- end}}

## 快速开始

```sh
appkit dev     # 生成 go.work，联调本地 appkit / 各域仓库（go.work 不提交）
make run-minimal # ★ 显式零依赖试跑（仅 /healthz /readyz /ping，数据面未启用）
make run-db    # 完整模式：自动起一次性开发 Postgres（docker）并注入 database.url
make migrate   # 只应用迁移然后退出（生产上这一步跑在 initContainer/Job 里）
make schema    # 从迁移重新生成 db/SCHEMA.md 与 db/schema/（{{if .Partitioned}}logical-template，非运行时分区检查{{else}}表结构总览 + ER 图{{end}}）
make check     # 架构检查（依赖方向 + SQL 跨 schema + 规则集漂移）
make lint      # golangci-lint + go-arch-lint（钉版本经 go run，无需预装）
make test
```

lint / CI 配置（.golangci.yml、.go-arch-lint.yml、.github/workflows/ci.yml）
已在 `appkit new` 时物化；升级 appkit 后跑 `appkit sync` 刷新，`make check` 与
CI 都会校验未漂移（改松规则不会让检查变绿）。CI 先验漂移、再按这份配置跑
`make lint` 的那两个检查器，版本与本地钉在同一个。
运行配置在 `config/dev.yaml`，任意键可被 `{{.EnvPrefix}}_*` 环境变量覆盖。
其中 `security.mode: disabled` 只供本地开发；任何监听 HTTP 的 staging/prod
进程都必须按暴露面显式选择严格模式并装配对应验签器，裸 `reg.Mount` 会在
严格模式下于监听前被拒绝。`user_facing` 配置用户验签器；
`internal_service/mixed` 必须配置 `security.service` 或通过
`bootstrap.MainWithSecurity` 注入 `authn.ServiceVerifier`，缺失时 fail closed。
服务 JWT 的接收方、可信签发方、公钥与服务主体必须显式列出，租户/分区委托
默认拒绝；详细契约见 AppKit 的 `docs/SERVICE_AUTH.md`。

**已应用的迁移不可再改**：历史表存着每个文件的 sha256，改动已应用的文件会让服务
以 `MIGRATION_DRIFT` 拒绝启动（错误信息里带修复命令）。改结构一律新增 `000N_*.sql`。
迁移因此是一条追加日志，读不出「这张表现在长什么样」——`make schema` 把它重放一遍，
产出 `db/SCHEMA.md`（表清单 + ER 图）与 `db/schema/<表>.{sql,md}`，提交进仓库。
{{- if .Partitioned}}
这些产物明确标为 logical-template，只描述共享的逻辑结构，不证明各运行时分区一致。
{{- end}}
建表时在迁移里写 `COMMENT ON TABLE`；文档从迁移中的说明生成，不另存一份事实源。

### 可选：让 sqlc 读取当前结构快照

`make schema-sqlc` 使用 `TEST_DATABASE_URL` 指定的一次性测试服务器，在新随机库
回放迁移，生成 `db/schema.sql` 和 `db/schema.lock.json`；不迁移连接串指向的库。
首次生成后，手动将 `sqlc.yaml` 的 `schema` 改为 `db/schema.sql` 并提交两个产物。
切换配置后才能继续生成或检查：仅支持一个 PostgreSQL SQL 项和一个快照输入；
`make schema-check` 与离线 `-check-source` 都会在连接数据库前验证这个配置。
以后改迁移先 `make schema-sqlc`，再 `make gen`；`make schema-check` 重放核验。
`make schema` 仍只生成文档和 ER 图，不能替代快照生成。

工具源码随脚手架提供，既有域可用支持该命令的 CLI 执行 `appkit schema-tool`
安装/更新；安装不改依赖、迁移、sqlc 配置或 Makefile，旧域可直接运行
`go run ./internal/postgres/schematool -domain {{.Name}}{{if .Partitioned}} -partitioned{{end}}`。
未采用快照时普通测试不强制快照专用配置；采用后测试校验来源/产物摘要，提供
测试 DSN 时还会真实重放。仅改哈希不能通过数据库验收。
快照是 sqlc/阅读输入，不是部署备份；只支持一个 PostgreSQL SQL 项和一个快照
输入，不含权限与数据，不支持的结构明确报错。迁移和测试服务器须可信。

## 结构

```
{{.Name}}.go             唯一导出面：func Module() —— 只被组合仓库与本仓库 cmd/ import
cmd/{{.Name}}d/          独立部署入口：只装本模块，外域契约绑 Remote client
internal/{{.Name}}/      业务包：领域类型 + 不变量 + 用例编排 + 所需接口（Store、Publisher）
internal/postgres/       全仓唯一允许 import pgx / sqlc 的包；实现业务包接口
internal/http/           transport：实现合约生成的 server 接口，只做 DTO 映射
internal/inbox/          外域事件消费者：inbox 去重后调业务包
internal/module/         装配层：Provide / Mount / Migrations / Health
db/migrations/           {{if .Partitioned}}全无前缀（落位由分区路由）；0001 为 appkit 基础设施表{{else}}只允许操作 "{{.Name}}" schema；0001 为 appkit 基础设施表{{end}}
db/SCHEMA.md             ★ 看表结构从这进：{{if .Partitioned}}逻辑模板总览 + ER 图{{else}}表清单 + ER 图{{end}}（make schema 生成，禁手改）
db/schema/               每表一份 DDL 与详情页；框架表隔在 _appkit/
db/queries/              sqlc 查询源；make gen 生成 internal/postgres/sqlc
```

## 约束（appkit sync 物化的机检 + appkit check）

1. `internal/{{.Name}}` 零 infra import——纯业务，禁止 pgx / gin / net/http；
2. pgx / sqlc 只出现在 `internal/postgres`——repo 层外无 SQL；
3. `internal/http`、`internal/inbox` 不得 import `internal/postgres`——
   transport 必须经业务包接口走业务逻辑。

另有仓库级铁律：域 repo 之间禁止互相 require，跨域调用只依赖
psp-contracts 生成的契约类型；跨模块一致性只走契约调用（可失败、须幂等）
或 outbox 事件，禁止跨模块共享事务与跨 schema JOIN。
