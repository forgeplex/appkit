# {{.Name}} —— AI Agent 操作规程

本仓库是基于 appkit 框架的业务域（`.appkit.yml` 是框架配置）。本文件是给
AI 编码代理的操作规程：照此行事，架构检查与 CI 会绿；绕开它们的路都被
工具链堵死了，不要尝试。
{{- if .Partitioned}}

## 分区域域（partitioned）——先读这段再动手

本域一套代码、N 份数据分区：schema 由调用方确定（已验证 principal 把租户
身份重建进 `callctx.Meta`；unsigned `X-Tenant-Id` 不可信。组合根注入
「租户 → schema」映射，见 `internal/module/module.go`）。
因此本仓库的 SQL 纪律与单 schema 域**恰好相反**：

1. `db/migrations/` 与 `db/queries/` 一律**不带 schema 前缀**——落位由
   事务级 `SET LOCAL search_path` 路由；带前缀会被 `make check` 拒绝，
   也会让 sqlc 编译失败；
2. 一切数据查询必须经 `tx.Do`（业务包的既有形态就是）——事务外
   `pgtx.From` 落在默认 search_path 上，无前缀表不存在即报错；
3. 分区不是本仓库的知识：加分区去组合根改映射 + 重启，迁移自动建
   schema，本仓库零改动；
4. `make schema` 不适用本形态（`appkit schema` 会明确报错）。
{{- end}}
{{- if .Tenant}}

## 租户域（tenant）——先读这段再动手

本域单 schema、行级隔离：全部租户的数据共表，`tenant_id` 列 + RLS 策略
在存储层隔开。三条纪律：

1. **建带租户的表照抄 `db/migrations/0002` 的形态**：`tenant_id text NOT NULL`
   列 + 以 tenant_id 打头的索引 + RLS 三件套（`pgtx.TenantPolicySQL` 的
   输出——ENABLE + FORCE + 策略）。漏挂三件套的表服务会拒绝启动
   （`pgtx.VerifyTenantRLS`，见 module.go 的 Setup）；
2. **业务查询照旧经 `tx.Do`**（骨架的既有形态就是）——租户身份由
   `pgtx.NewTenant` 的事务自动落成 GUC；绕开事务直查业务表会被策略函数
   响亮拒绝（`app.tenant_id 未设置`），这是特性不是故障；
3. **租户身份只从 `callctx.From(ctx).TenantID` 取**——authn 已从令牌 tid
   焊入，业务代码读请求头即伪造面。

隔离在存储层强制的含义：SQL 里漏写租户 WHERE，结果是查不到别的租户的行
/ 跨租户写入被拒，而不是静默泄漏。
{{- end}}

## 仓库地图（谁负责什么）

| 路径 | 职责 | 你会经常改吗 |
|---|---|---|
| `internal/{{.Name}}/` | ★ 业务包：领域类型、不变量、用例编排、所需接口 | 是，主战场 |
| `internal/postgres/` | Store 实现；**全仓唯一允许 import pgx/sqlc 的包** | 是 |
| `internal/postgres/sqlc/` | sqlc 生成物 | **禁止手改**（make gen 重新生成） |
| `internal/http/` | handler：只做 DTO ↔ 领域类型映射 | 是 |
| `internal/inbox/` | 外域事件消费者 | 偶尔 |
| `internal/module/module.go` | 唯一 wiring 落点（Provide/Mount/Migrations/Health） | 每加路由/用例改一行 |
| `db/migrations/` | 迁移；`0001_appkit_base.sql` 是框架基础表 | 新增文件，**不改旧文件** |
{{- if not .Partitioned}}
| `db/SCHEMA.md`、`db/schema/` | 表结构总览 + ER 图 + 每表详情，从迁移派生 | **禁止手改**（make schema 重新生成） |
{{- end}}
| `db/queries/` | SQL 唯一存在地（sqlc 源） | 是 |
| `{{.Name}}.go`、`cmd/` | 导出面与部署入口 | 几乎不 |
| `.golangci.yml`、`.go-arch-lint.yml`、`.github/` | appkit sync 物化的规则 | **禁止手改**（appkit sync 刷新） |

## 三条方向性约束（lint + appkit check 机检，违规 = CI 红）

1. `internal/{{.Name}}` 零 infra import：禁 pgx / gin / net/http；
2. pgx / sqlc 只出现在 `internal/postgres`；
3. `internal/http`、`internal/inbox` 禁止 import `internal/postgres`——
   必须经业务包接口走逻辑。

## 加一个功能 = 固定六步（按序执行）

以「创建用户」为例：

1. **迁移** `db/migrations/000N_users.sql`：四位序号严格递增；{{- if .Partitioned}}
   **不带 schema 前缀**（落位由分区路由）；{{else}}只操作
   `{{.Name}}.` schema；{{end}}已应用的迁移文件**是**不可变的——历史表存着内容 sha256，
   改动会让服务以 `MIGRATION_DRIFT` 拒绝启动，改表结构永远新开文件。
{{- if .Tenant}}
   带租户数据的表照抄 `0002_demo_notes.sql` 的形态（tenant_id 列 + 租户打头
   索引 + RLS 三件套），漏挂三件套服务会拒绝启动。
{{- end}}
{{- if .Partitioned}}
   建表时在同一个迁移里写 `COMMENT ON TABLE users IS '…'`——表的用途
   属于 schema 设计，写在迁移里才会跟着表一起演进，写在外部文档里必腐。
   （schema 文档暂不支持分区域形态，COMMENT 就是当前的自描述手段。）
{{- else}}
   动手前先读 `db/schema/<表>.sql` 看这张表**现在**长什么样——迁移是追加日志，
   一张表的真实形状散在 N 个文件里，别在脑子里重放整条历史。
   建表时在同一个迁移里写 `COMMENT ON TABLE {{.Name}}.users IS '…'`——表的用途
   属于 schema 设计，写在迁移里才会跟着表一起演进，写在外部文档里必腐；缺了会在
   `db/SCHEMA.md` 里被标 ⚠，CI 的 `appkit schema -check` 会逐表打 ::warning
   注解（不阻断合并，但 review 与 PR 里都看得见）。
   写完跑 `make schema` 重新生成 schema 文档，产出一并提交。
{{- end}}
2. **SQL** `db/queries/xxx.sql`：sqlc 注释语法（`-- name: InsertUser :exec`），
   {{- if .Partitioned}}表名不带 schema 前缀{{else}}表名带 `{{.Name}}.` 前缀{{end}}。然后 `make gen`。
3. **业务包** `internal/{{.Name}}/`：
   - 领域类型 + 校验（纯函数，错误用 `apperr`）；
   - `store.go` 的 Store 接口加方法（签名只出现领域类型）;
   - `errors.go` 加错误模板（`apperr.New("{{.Upper}}_XXX", 状态码, 消息)`）；
   - `service.go` 加用例方法，**标准形态**：
     ```go
     func (s *Service) CreateUser(ctx context.Context, in CreateUser) (User, error) {
         if err := in.validate(); err != nil { return User{}, err }  // ① 不变量
         u := newUser(in)
         err := s.txr.Do(ctx, func(ctx context.Context) error {      // ② 事务边界
             if err := s.store.InsertUser(ctx, u); err != nil { return err }
             return s.pub.Publish(ctx, evt)                          // ③ 事件同事务
         })
         return u, err
     }
     ```
4. **Store 实现** `internal/postgres/store.go`：
   `sqlc.New(pgtx.From(ctx, s.pool)).InsertUser(...)`——`pgtx.From` 自动感知
   事务，不要传递任何事务对象。唯一约束冲突（pgconn.PgError 23505）在这里
   翻译成域错误；除此之外本包不做业务判断。
5. **handler** `internal/http/`：解码 → 调 Service → 编码；错误一律
   `httpserver.WriteError(log, w, err)`（自动 RFC 9457）。
6. **挂路由** `internal/module/module.go`：Setup 里构造 handler，Register 里
   显式声明根路由的安全分类：公开端点用 `reg.MountPublic`，只要求登录用
   `reg.MountAuthenticated`，绑定权限码用
   `reg.MountPermission("POST /users", "{{.Name}}:write", handler)`，服务间端点
   用 `reg.MountInternalService`。禁止新写裸 `reg.Mount`——严格安全模式会在
   监听前拒绝未分类路由；模块内部子 mux 无法用 Mount 系列时才用
   `reg.Require(code, handler)` 包裹。

## 惯用法（别自己发明）

- **HTTP 安全模式**：每个监听进程必须显式配置 `security.mode`；生成的 dev
  配置用 `disabled` 只为本地开发，staging/prod 禁用它。模块只负责给每条根
  路由选安全分类，组合根负责按部署暴露面选择模式和装配验签器；不要把“内网”
  当作身份校验。当前 bootstrap 阶段只有 `user_facing` 可在配好
  `AuthnPublicKey`/`AuthnIssuer` 后启动；服务 JWT 验证器落地前，
  `internal_service/mixed` 会有意 fail closed；
- **金额**：存储与运算用 `decimal.Decimal`（sqlc 已把 NUMERIC 全局 override
  成它，无需注册任何 codec）；需要把"币种+金额"绑在一起时才用 `money.Money`
  （领域层值对象，不落库——币种另列存）。JSON/合约边界金额一律字符串，
  入站解析用 `money.ParseCanonical`（规范形态：拒 "+80"/"080"/"8e1"/负零，
  同值同串，幂等指纹与签名才不歪）。禁 float、decimal 不上 JSON 面都有
  机检：`make lint` 与 CI 的 appkit-lint（moneyfloat/decjson，默认只查
  生产代码，`-xxx.tests=true` 连测试一起查）；
- **错误**：跨层只传 `*apperr.Error`，判定用 `apperr.Is(err, code)`，
  禁止字符串比对；
- **事件**：只能在 `tx.Do` 内经注入的 Publisher 发（落 outbox，提交后
  relay 自动外发）；事务外发布会被运行时守卫拒绝；
- **消费事件**：`reg.Consumer(topic, outbox.Inbox(pool, schema, consumer, h))`，
  handler 必须幂等；
- **跨域调用**：只依赖合约 module 的接口 + `appkit.Resolve`；
  **禁止 require 其他业务域仓库**（appkit check 会拦）；事务内跨域调用
  会得到 TX_BOUNDARY 错误——一致性走事件，不走共享事务；
- **写接口幂等**：套 `idem.Middleware`，客户端带 `Idempotency-Key` 头；
  金额入站走 `money.ParseCanonical` 的域，把同款规范化口径接进
  `idem.WithCanonicalizer`（否则 "80"/"80.00" 的重试撞 422）；多租户/多账本
  用 `idem.WithKeyScope` 隔离键空间，别手拼前缀；
- **后台任务**：`reg.Worker(name, run)`，**禁止自己 `go func()`**——框架负责
  起协程、关停等待、异常退出上报，手写的那三件事每次都会漏一件；
- **周期任务**：`reg.Worker(name, job.Every(pool, job.Task{Name: "{{.Name}}.xxx",
  Interval: ..., Run: ...}))`。裸 `time.Ticker` 在多副本上就是每轮重复执行，
  轻则浪费重则重复入账；
- **指标**：契约调用、outbox 投递与死信、周期任务、数据库查询已由框架自动埋点，
  不要重复埋；要加自己的指标用 `otel.Meter("{{.Module}}")` 新建，
  **不要往框架指标上加标签**（基数会失控）；
- **request id / 租户**：读 `callctx.From(ctx)`，框架已在 HTTP 入站、契约调用、
  事件 meta 三条路径上自动传播（出站 HTTP client 要自己装 `callctx.Transport`，
  但那是合约仓库的事，本仓库不写 client）；自己往 ctx 塞的值跨契约调用时会被
  防火墙剥掉。

## 命令

```sh
make gen        # sqlc 生成（改了 db/queries 后必跑）
{{- if not .Partitioned}}
make schema     # 重新生成 db/SCHEMA.md 与 db/schema/（改了迁移后必跑，需 docker）
{{- end}}
make run        # 正常起服；database.url 缺失时 fail closed
make run-minimal # 显式无 DB 最小模式（仅 dev）
make dev-db     # 一次性本地 Postgres（与 dev.yaml 匹配）
make migrate    # 只应用迁移然后退出（生产上跑在 initContainer/Job 里）
make check      # 架构检查：依赖/import 边界/跨 schema SQL/迁移序号/规则集漂移
make lint       # golangci-lint + go-arch-lint（版本与 CI 同源，本地绿 = CI 绿）
make test       # 全部测试（集成测试需 TEST_DATABASE_URL，缺省自动 skip）
appkit dev      # 联调本地未发布的 appkit（生成 go.work，不提交）
appkit sync     # 升级 appkit 后刷新 lint/CI 配置
appkit doctor   # 环境体检（GOPRIVATE/git 凭据/工作区）
```

## 测试要求（三层，新功能三层都要有）

1. 业务包单测：手写假 Store（实现接口的 struct），测不变量与错误路径，零 DB；
2. handler 测试：httptest 直打，断言状态码与 problem+json 的 `code`；
3. 集成测试：env-guard `TEST_DATABASE_URL`，测约束翻译与事务原子性。

本域对外提供契约（`appkit.ProvideContract`）时再加一层：`apptest.Conform` 把同一批
用例分别跑过 `WrapXxx(impl, 0)` 与指向 `httptest.Server` 的生成 client，比对错误码、
返回值与边界语义。这是"拆成微服务行为不变"唯一的证明方式，不写就只有口头承诺。
每个 `Binding` 顺手填上 `SeenMeta`（两个绑定共用一个记录 `callctx.From(ctx)` 的
spy），把跨边界元数据也纳进来——远程路径跨网络后必须经已验证 principal
重建，unsigned header 只负责传输、不授信。漏了的症状是进程内拿得到租户、
远程拿不到，只在真拆分那天暴露。这项检查跑在声明了
`Idempotent` 的成功用例上（它要额外发一次真实调用），至少留一条。
文件放 `internal/http/`——`internal/{{.Name}}/` 连测试文件也禁 `net/http`（depguard 会拦）。

## 完成的定义

`make gen`（如改过 SQL）{{if not .Partitioned}}与 `make schema`（如改过迁移）{{end}}之后，
**`make check && make lint && make test` 全绿**才算完成
（这三条就是 CI 跑的那几条，本地绿基本等于 CI 绿）。
{{- if not .Partitioned}}
schema 文档漂移由 CI 单独一步 `appkit schema -check` 拦，忘了重新生成会红。
{{- end}}
不要用 `//nolint` 绕过检查（必须写理由且 review 会拒）；不要修改生成物、
已应用的迁移、`.golangci.yml` 等物化配置来让检查通过——那是在破坏约束本身，
正确做法永远是让代码符合约束。
