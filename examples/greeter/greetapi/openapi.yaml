# Code generated by appkit gen. DO NOT EDIT.
# 事实源是 contract.yaml；本文件是派生导出，供文档与 oasdiff 门禁消费。
openapi: 3.1.0
info:
  title: "greet 契约"
  version: "1"
paths:
  "/greet":
    post:
      operationId: "Greet"
      description: "打招呼。lang 空值等价 \"en\"；不支持的语言返回 GREET_UNSUPPORTED_LANG。"
      x-idempotent: true
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/GreetRequest"
      responses:
        "200":
          description: 成功
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/GreetReply"
        default:
          description: 错误（错误身份 = code 扩展成员，跨网络重建后 apperr.Is 判定不变）
          content:
            application/problem+json:
              schema:
                $ref: '#/components/schemas/Problem'
components:
  schemas:
    GreetRequest:
      type: object
      description: "Greet 的请求 DTO。"
      properties:
        "name":
          type: string
        "lang":
          type: string
      required:
        - "name"
    GreetReply:
      type: object
      description: "Greet 的响应 DTO。"
      properties:
        "message":
          type: string
    Problem:
      type: object
      description: RFC 9457 problem+json（apperr.Problem）。
      properties:
        type:
          type: string
        title:
          type: string
        status:
          type: integer
        detail:
          type: string
        code:
          type: string
          description: 错误身份；客户端据此重建 *apperr.Error。
        details:
          type: object
      required:
        - title
        - status
        - code
